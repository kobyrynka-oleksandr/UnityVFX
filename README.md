Swords task in (Assets -> CreativeCore_VFX -> Scenes -> SwordScene)

ToggleParticleController (Assets -> CreativeCore_VFX -> Scripts -> ToggleParticleController) - script that toggles a list of ParticleSystems on/off with a single key press (default: Space). When active, it stops all systems in the particles list. When toggled back on, it first plays corresponding systems from initializationParticles (if available), then resumes all particles.
